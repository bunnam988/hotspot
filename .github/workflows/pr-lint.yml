name: PR Format Check

on:
  pull_request_target:
    types: [opened, edited, synchronize, reopened]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  lint:
    uses: bunnam988/build_tools_workflows/.github/workflows/pr-lint.yml@develop
